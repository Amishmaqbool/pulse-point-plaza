name: Build and Deploy to Nexlayer

on:
  push:
    branches:
      - main
  workflow_dispatch: # Allow manual triggering

env:
  FRONTEND_IMAGE_NAME: pulse-point-plaza-frontend

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry
        id: login
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
        continue-on-error: true

      - name: Check registry credentials
        if: steps.login.outcome == 'failure'
        run: |
          echo "::warning::Failed to login to ghcr.io"
          echo "The workflow will build the image but cannot push to registry."

      # Frontend Docker image build
      - name: Extract frontend metadata for Docker
        id: frontendMeta
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/amishmaqbool/${{ env.FRONTEND_IMAGE_NAME }}
          tags: |
            type=sha,prefix=,format=short
            type=raw,value=latest,enable={{is_default_branch}}

      - name: Build and push frontend Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Dockerfile
          push: ${{ steps.login.outcome == 'success' }}
          platforms: linux/amd64
          tags: ${{ steps.frontendMeta.outputs.tags }}
          labels: ${{ steps.frontendMeta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Update nexlayer.yaml
        if: steps.login.outcome == 'success'
        run: |
          # Extract the short SHA
          SHORT_SHA=$(echo ${{ github.sha }} | cut -c1-7)
          
          # Update the frontend image in nexlayer.yaml to use ghcr.io (where we pushed)
          sed -i "s|image:.*|image: ghcr.io/amishmaqbool/${{ env.FRONTEND_IMAGE_NAME }}:$SHORT_SHA|" nexlayer.yaml
          
          # Display the updated file for verification
          cat nexlayer.yaml

      - name: Deploy to Nexlayer
        if: steps.login.outcome == 'success'
        env:
          NEXLAYER_SESSION_TOKEN: ${{ secrets.NEXLAYER_SESSION_TOKEN }}
        run: |
          # Deploy to Nexlayer using the API
          curl -X POST https://app.nexlayer.io/startUserDeployment/$NEXLAYER_SESSION_TOKEN \
            -H "Content-Type: text/x-yaml" \
            --data-binary @nexlayer.yaml

      - name: Lovable Deployment Output
        if: steps.login.outcome == 'success'
        run: |
          echo "LOVABLE_DEPLOY_URL=https://pulse-point-plaza.nexlayer.app" >> $GITHUB_ENV
          echo "LOVABLE_DEPLOY_STATUS=success" >> $GITHUB_ENV
