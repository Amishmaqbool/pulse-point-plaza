application:
  name: pulse-point-plaza
  registryLogin:
    registry: ghcr.io
    username: amishmaqbool
    personalAccessToken: <NEXLAYER_TOKEN>
  pods:
    - name: frontend
      image: <% REGISTRY %>/amishmaqbool/pulse-point-plaza-frontend:latest
      path: "/"
      servicePorts: [80]
      vars:
        NODE_ENV: production
